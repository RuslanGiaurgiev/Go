// Первые модули и импорты внешних библиотек
// Так же разбор с F строкой

package main

import (
	"fmt"
)

func main() {
	name := "Artur"
	surname := "Safik"
	age := 18.8
	how := "V kvadrate"

	fmt.Printf("%s %s who is age = %.1f loh %s \n", name, surname, age, how)

}
